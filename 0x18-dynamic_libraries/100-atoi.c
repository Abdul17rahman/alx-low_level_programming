#include "main.h"

/**
 * _atoi - converts a string to an int
 * @s: s is the pointer to a string
 *
 * Description: This function converts a string to an int
 * Return: interger
 */

int _atoi(char *s)
{
	return (atoi(s));
}
